import { HiShieldCheck } from "react-icons/hi"
import { MdCancel, MdAnalytics } from "react-icons/md"
const data = [
    {

        icon: <HiShieldCheck />,
        heading: "Best internet in the market",
        deatiles: " attribute requirea valid, navigable address as the href value. If you cannot but still need the element to resemble a link, use a button and change "

    },
    {

        icon: <MdCancel/>,
        heading: "Best internet in the market",
        deatiles: " attribute requires valid, navigable address asthe hr If you cannot but still need the element to resemble a link, use a button and change "

    },
    {

        icon: <MdAnalytics />,
        heading: "Best internet in the market",
        deatiles: " attribute requires a valid value to be accessible. Provide a valid, navigable address as the href value. If you cannot but still need the element to resemble a link, use a button and change "

    },

]

export default data